/*using Battle.Model.Attack;
using Battle.Model.Player;
using Battle.View;
using UnityEngine;

namespace Battle.Test
{
    public class AttackActiveTest : MonoBehaviour
    {
        private void Start()
        {
            var attackView = GetComponent<IAttackView>();
            attackView.SetUp(new AttackParameter(new PlayerKey()));
            attackView.Activate(true);
        }
    }
}*/